name: safetime
description: Trusted time access for Dart and Flutter apps using NTP plus monotonic clocks.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.6.2 <4.0.0"

# Add regular dependencies here.
dependencies:
  ffi: 2.1.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
